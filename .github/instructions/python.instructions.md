---
applyTo: "samples/python-ticket-analyzer/**/*.py"
---

Keep the Python sample compatible with `uv`, simple to run, and easy to explain.

Prefer standard library plus the already-declared dependencies.

Favor straightforward typing, pure helper functions, and readable CLI behavior.

When adding features from a spec, preserve the existing command style and add pytest coverage.
